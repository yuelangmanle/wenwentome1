# 开发进度书

## 当前阶段

- 阶段：`1.3` 发版准备完成
- 当前基线：`1.3` 待 GitHub 正式发布
- 当前目标：收口 `1.3` 发版，并转入 `1.4` 规划，继续推进书源兼容底座、PDF 阅读后置能力、阅读体验深化、API Hub 扩展与网文链路补强

## 已完成

- 统一书库、本地 TXT / EPUB 阅读
- 详情页与阅读状态持久化
- 发现页、书源导入、加入书库
- GitHub 私有仓库同步 MVP
- Discover 搜索竞态与重复入库硬化
- 旧快照书源字段兼容修复
- App 内 changelog 基础
- “我的”页关于与项目入口
- `main` 默认分支建立
- `v1.0` GitHub Release 与签名 APK 发布完成
- `1.1 / Task 7` 书架重构完成，书籍卡片、封面操作与状态展示已切到新版 UI
- `1.1 / Task 8` 详情页四段结构完成，首屏主阅读按钮、目录当前章/最新章识别和封面管理区已收口
- 阅读器三模式、目录、章节跳转、继续阅读主链已接通
- 网文阅读保存进度后回流书架的 app 闭环测试已补齐
- `WEB` 阅读 locator 已升级为“章节 + 段落”精度，继续阅读可恢复到章内位置
- `1.2 / Task 5` API Hub 总览、Provider 管理、模型绑定、预算回退、价格目录与调用记录页已落地
- `1.2 / Task 5` 预算保护、价格覆盖、fallback 日志、usage log 与数据库迁移完成
- `1.2 / Task 6` TXT / EPUB / WEB 阅读链路回归，阅读模式切换保留进度，EPUB 优先正文
- `1.2 / Task 6` 书架筛选排序与阅读器目录 / 进度 / 翻页 / 滚动体验收口
- `1.2 / Task 7` 阅读器 AI 面板、章节总结、段落解释、翻译与 TTS 入口接通
- `1.2 / Task 7` API Hub 能力绑定驱动、结果缓存复用与错误态提示完成
- `1.2 / Task 8` 详情页 AI 元数据增强、推荐封面建议与封面管理三模式并存
- `1.2 / Task 8` 元数据增强结果缓存落地
- `1.2 / Task 9` 书源健康分、发现页健康 badge、搜索排序增强与自动切源提示完成
- `1.2 / Task 9` 刷新目录失败自动切换候选书源并同步更新状态
- Release 元数据脚本已补到版本包一致性校验
- GitHub CLI 已连通，可直接从当前环境触发云端验证与查看 run
- `release/android-reader-v1` 已合并到 `main`
- `v1.2` tag、GitHub Release、GitHub Pages 发布页与云端 `app-release.apk` 已完成发布
- Release APK 已成功落地到本地归档，便于直接分发和回归验证
- `1.3 / Task 1` 发现页搜索与选书桥接请求改为 `IO` 线程执行，避免主线程阻塞
- `1.3 / Task 1` TXT / EPUB 阅读进度重算为“全文段落数 + 当前窗口起点”模型，修复本地进度失真
- `1.3 / Task 1` 阅读器顶部进度条、文案、设置面板与目录面板统一跟随当前视口实时更新
- `1.3 / Task 1` Release 元数据校验已纳入发布页版本一致性检查，android-release 手动触发规则收紧
- `1.3 / Task 1` GitHub Actions JavaScript actions 已切到 Node 24 运行时

## 当前风险

- `1.3` 发版准备已完成，以下风险均为后续迭代项，不阻塞当前版本交付
- app 级回归仍以 `WEB` 主链为主，`TXT/EPUB` 的端到端烟雾覆盖仍可继续补宽
- 当前书源兼容底座仍只覆盖较基础规则，`书源.json` 中大量 `@js:` / `$` / `##` 规则尚未完整兼容
- PDF 阅读仍未实现，这一项按现阶段规划继续后置到 `1.4+`
- Release 签名沿用仓库现有 GitHub secrets，暂未做签名轮换，以避免升级兼容风险
- GitHub Actions 依赖链已完成 Node 24 收口，但缓存策略与签名轮换仍可继续整理

## 下一步

1. `1.4` 第一优先级：补书源兼容底座，分批支持 `@js:`、`$`、`##` 与 `<js>` 等高频规则
2. 阅读体验继续深化：补 PDF 阅读链路、更多阅读主题、排版配置、仿真翻页细节和更完整的端到端回归
3. API Hub 按路线图继续推进：阅读问答、多段落批量翻译、书摘总结卡片、Provider 远端模型发现、调用日志导出
4. 网文链路继续补强：书源健康仪表板、自动切源策略面板、发现页推荐与刷新策略优化
5. 发布链路继续完善：把云端构建产物、版本信息和 App 内更新页串得更完整，减少手工发布步骤
